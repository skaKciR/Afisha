@model Ticket


<div class="container">
    <div class="row">

        <div class="col-xs-12 col-md-12">
            <div>
                <h1 style="font-size: 5vmin;color:black;">@Model.Id</h1>
                <h1 style="font-size: 5vmin;color:black;">@Model.UserName</h1>
                @*<h2 style="font-size: 3.5vmin;color: gray;">@Model.Type @Model.Age+</h2>
                @if(Model.Age>=18){
                    <h2 style="font-size: 3.5vmin;color: red;">Внимание ! Проверьте возраст</h2>
                }
            </div>
            <img style="max-height:520px;border-radius: 2vmin;min-height: 170px;filter:brightness(0.8);box-shadow: 0px 5px 9px 3px #000000c4;" src="~/images/@Model.TitleImagePath" class="d-block w-100" alt=""> 
            <form style="margin-top: 4vmin;text-align: center;" asp-controller="Cart" asp-action="AddToCart" asp-route-eventId="@Model.Id" asp-route-userName="@User.Identity.Name">
                <button type="submit" style="margin-top: 1vmin;border-radius: 3vmin;background: black;color: white;font-size: 2.5vmin;">Подтвердить</button>
            </form>*@
        </div>
         
    </div>
</div>




